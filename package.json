{
  "name": "tabber",
  "version": "0.7.0",
  "description": "Simple tab management",
  "main": "dist/popup.html",
  "keywords": [
    "extension",
    "chrome"
  ],
  "watches": {
    "build:js": "src/js/**",
    "build:css": "src/sass/**",
    "build:html": "src/*.html"
  },
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "clean": "rimraf dist",
    "build:js": "node rollup.js",
    "build:css": "node-sass src/sass -o dist/css",
    "build:fonts": "cpx 'src/fonts/*.{otf,ttf,woff,woff2}' dist/css/fonts",
    "build:html": "cpx 'src/*.html' dist",
    "build": "npm-run-all clean build:*",
    "watch": "rerun-script",
    "start": "npm-run-all build -s -p watch"
  },
  "author": "Gürhan Mermer <gmermer@gmail.com>",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@babel/core": "^7.1.5",
    "@babel/preset-env": "^7.1.5",
    "cpx": "^1.5.0",
    "node-sass": "^4.10.0",
    "npm-run-all": "^4.1.3",
    "rerun-script": "^0.6.0",
    "rimraf": "^2.6.2",
    "rollup": "^0.67.0",
    "rollup-plugin-babel": "^4.0.3",
    "rollup-plugin-uglify": "^6.0.0"
  },
  "dependencies": {}
}
